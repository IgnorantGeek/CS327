#include <string.h>
#include <stdio.h>
#include <stdlib.h>

#include "EscapeTheRoom.h"

int pickupItem(item_t *i)
{
    return 0;
}

int useItem(item_t *i)
{
    return 0;
}

int openContainer(container_t *c)
{
    return 0;
}