#ifndef DATA_H
 #define DATA_H

class data
{
    //can be left empty for now. This data type should never ACTUALLY get used, just serves as a base class 
    //so multiple types of data can be stored in an action based on needs. Such as a bolean value or reference
    //to another object
};

#endif